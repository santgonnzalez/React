import cartLogo from './asset/carrito.svg'

const CartWidget = () => {
    return(
        <div>
            <img src={cartLogo} alt="cart widget" style={{width: '50px',height:'50px'}}/>{0}
        </div>
    )
}

export default CartWidget;